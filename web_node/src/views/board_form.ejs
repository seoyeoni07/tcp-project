<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <title><%= mode === 'edit' ? '글 수정' : '글 쓰기' %></title>
  <link rel="stylesheet" href="/css/design-system.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="/css/nav.css" />
</head>
<body>
  <%- include('partials/nav', { title, user }) %>

  <div class="board-wrap">
    <h2>글 <%= mode === 'edit' ? '수정' : '쓰기' %></h2>

    <div class="board-meta">
      <% if (post && post.created_at_fmt) { %>
        <span>작성일: <%= post.created_at_fmt %></span>
      <% } %>
      <% if (post && post.updated_at_fmt) { %>
        <span>수정일: <%= post.updated_at_fmt %></span>
      <% } %>
    </div>

    <form
      method="post"
      enctype="multipart/form-data"
      action="<%= mode === 'edit' && post ? ('/board/' + post.post_id + '/edit') : '/board/new' %>"
    >
      <input
        class="input-text"
        type="text"
        name="title"
        value="<%= post ? post.title : '' %>"
        placeholder="제목"
        required
      />

<textarea class="input-textarea" name="content" required><%= post ? post.content : '' %></textarea>

      <% const isAdmin = user && user.role === 'admin'; %>
      <% if (isAdmin) { %>
        <div class="form-row">
          <label>
            <input
              type="checkbox"
              name="is_notice"
              value="1"
              <%= post && post.is_notice ? 'checked' : '' %>
            />
            공지글로 등록
          </label>
        </div>
      <% } %>

      <div class="form-row">
        <label>첨부 파일</label>
        
        <% if (mode === 'edit' && post && post.file_original) { %>
          <div class="current-file">
            <div class="file-info">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
                <path d="M9 0H4a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V4.5L9 0z"/>
                <path d="M9 4.5V0l4.5 4.5H9z"/>
              </svg>
              <span class="file-name"><%= post.file_original %></span>
              <% if (post.file_size) { %>
                <span class="file-size">(<%= (post.file_size / 1024).toFixed(1) %> KB)</span>
              <% } %>
            </div>
            
            <label class="delete-file-option">
              <input type="checkbox" name="delete_file" value="1" id="deleteFileCheckbox" />
              기존 파일 삭제
            </label>
          </div>
          
          <div class="help-text" style="margin-top: 12px;">
            💡 새 파일을 선택하면 기존 파일이 교체됩니다
          </div>
        <% } %>
        
        <input type="file" name="attachment" id="fileInput" />
      </div>

      <div style="display: flex; gap: 10px; justify-content: flex-end; margin-top: 8px;">
        <a href="/board" class="btn btn-secondary">목록</a>
        <button type="submit" class="btn btn-primary">
          <%= mode === 'edit' ? '수정' : '등록' %>
        </button>
      </div>
    </form>

  <style>
    body {
      background: var(--bg-page);
    }

    .board-wrap {
      max-width: 800px;
      margin: 40px auto;
      padding: 40px;
      background: white;
      border-radius: var(--radius-lg);
      box-shadow: var(--shadow-sm);
    }

    .board-wrap h2 {
      font-size: 24px;
      font-weight: 700;
      color: var(--text-primary);
      margin: 0 0 16px 0;
    }

    .board-meta {
      display: flex;
      gap: 16px;
      padding: 12px 0;
      border-bottom: 1px solid var(--border-color);
      margin-bottom: 28px;
      font-size: 14px;
      color: var(--text-tertiary);
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .input-text {
      width: 100%;
      padding: 12px 16px;
      border: 1px solid var(--border-color);
      border-radius: var(--radius-md);
      font-size: 15px;
      outline: none;
      transition: border-color 0.2s;
    }

    .input-text:focus {
      border-color: var(--primary);
    }

    .input-textarea {
      width: 100%;
      min-height: 400px;
      padding: 16px;
      border: 1px solid var(--border-color);
      border-radius: var(--radius-md);
      font-size: 15px;
      line-height: 1.8;
      resize: vertical;
      outline: none;
      transition: border-color 0.2s;
      font-family: inherit;
    }

    .input-textarea:focus {
      border-color: var(--primary);
    }

    .form-row {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .form-row label {
      font-size: 14px;
      font-weight: 600;
      color: var(--text-primary);
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .form-row input[type="checkbox"] {
      width: 18px;
      height: 18px;
      cursor: pointer;
    }

    .form-row input[type="file"] {
      padding: 8px 0;
      font-size: 14px;
      cursor: pointer;
    }

    .current-file {
      background: var(--bg-secondary);
      border: 1px solid var(--border-color);
      border-radius: var(--radius-md);
      padding: 16px;
      margin-bottom: 12px;
    }

    .file-info {
      display: flex;
      align-items: center;
      gap: 8px;
      color: var(--text-secondary);
      margin-bottom: 12px;
    }

    .file-info svg {
      flex-shrink: 0;
      color: var(--primary);
    }

    .file-name {
      font-size: 14px;
      font-weight: 500;
      color: var(--text-primary);
    }

    .file-size {
      font-size: 13px;
      color: var(--text-tertiary);
    }

    .delete-file-option {
      display: flex;
      align-items: center;
      gap: 8px;
      padding: 8px 12px;
      background: white;
      border: 1px solid var(--border-color);
      border-radius: var(--radius-sm);
      cursor: pointer;
      transition: all 0.2s;
      font-size: 14px;
      font-weight: 500;
      color: var(--text-secondary);
      width: fit-content;
    }

    .delete-file-option:hover {
      background: #fee;
      border-color: #fcc;
    }

    .delete-file-option input[type="checkbox"] {
      margin: 0;
    }

    .delete-file-option input[type="checkbox"]:checked + span {
      color: #dc2626;
    }

    .help-text {
      font-size: 13px;
      color: var(--text-tertiary);
      margin-top: 6px;
    }

    .btn {
      padding: 12px 24px;
      border: 1px solid var(--border-color);
      border-radius: var(--radius-md);
      background: white;
      color: var(--text-secondary);
      font-size: 14px;
      font-weight: 600;
      text-decoration: none;
      cursor: pointer;
      transition: all 0.2s;
      display: inline-block;
    }

    .btn:hover {
      background: var(--bg-secondary);
    }

    .btn-primary {
      background: var(--primary);
      border-color: var(--primary);
      color: white;
    }

    .btn-primary:hover {
      background: #1d4ed8;
      border-color: #1d4ed8;
    }

    .btn-secondary {
      background: var(--bg-secondary);
    }
  </style>

  <script>
    const deleteCheckbox = document.getElementById('deleteFileCheckbox');
    const fileInput = document.getElementById('fileInput');

    if (deleteCheckbox && fileInput) {
      fileInput.addEventListener('change', function() {
        if (this.files && this.files.length > 0) {
          deleteCheckbox.checked = false;
        }
      });

      deleteCheckbox.addEventListener('change', function() {
        if (this.checked) {
          fileInput.value = '';
        }
      });
    }
  </script>
</head>
</body>
</html>
