<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/design-system.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <link rel="stylesheet" href="/css/nav.css" />
    <style>
        .list-container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .list-group {
            list-style: none;
            padding: 0;
        }
        .list-group-item {
            border: 1px solid #ddd;
            padding: 15px;
            margin-bottom: 10px;
            border-radius: 4px;
            transition: background-color 0.3s;
        }
        .list-group-item:hover {
            background-color: #f5f5f5;
        }
        .item-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 5px;
        }
        .item-title a {
            font-size: 1.2em;
            font-weight: bold;
            color: #111;;
            text-decoration: none;
        }
        .item-details {
            font-size: 0.9em;
            color: #666;
        }
        .item-details span {
            margin-right: 15px;
        }

        .btn-back {
            display: inline-block;
            padding: 6px 14px;
            font-size: 14px;
            border: 1px solid #ccc;
            border-radius: 6px;
            background: #fafafa;
            color: #333;
            text-decoration: none;
        }

        .btn-back:hover {
            background: #fafafa;
        }
    </style>
</head>
<body>
    <%- include('../partials/nav', { title, user, active }) %>

    <div class="container mt-5">
        <div class="list-container">
            <h2 class="mb-4"><%= date %> 업무일지 목록 (총 <%= logs.length %>건)</h2>
            <hr>
            
            <% if (logs.length > 0) { %>
                <ul class="list-group">
                    <% logs.forEach(log => { %>
                        <li class="list-group-item">
                            <div class="item-header">
                                <div class="item-title">
                                    <a href="/worklog/<%= log.log_id %>"><%= log.title %></a>
                                </div>
                                <span class="badge badge-secondary"><%= log.author_name %></span>
                            </div>
                            <div class="item-details">
                                <span>출근: <%= log.start_time %></span>
                                <span>퇴근: <%= log.end_time %></span>
                                <span>근무시간: <%= log.work_time %></span>
                            </div>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p class="text-center text-muted">해당 날짜에 작성된 업무일지가 없습니다.</p>
            <% } %>
            
            <a href="/worklog?year=<%= date.substring(0, 4) %>&month=<%= date.substring(5, 7) %>" 
                class="btn-back mt-3">
                목록으로
            </a>
        </div>
    </div>
</body>
</html>